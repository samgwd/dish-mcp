[project]
name = "dish-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.13.1",
    "pip>=25.3",
    "python-dotenv>=1.0.0",
    "requests>=2.32.5",
]
